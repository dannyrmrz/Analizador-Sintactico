/* Example grammar for LL(1) preprocessing. */

%token NUMBER PLUS TIMES LPAREN RPAREN WS
IGNORE WS

%%

expr:
    term exprp
;

exprp:
    PLUS term exprp
  | epsilon
;

term:
    factor termp
;

termp:
    TIMES factor termp
  | epsilon
;

factor:
    NUMBER
  | LPAREN expr RPAREN
;

